// main_inference.cpp — Standalone test entry point (not used in iOS app)
// The iOS app uses SenseVoiceContext bridge instead.

#include <iostream>

int test_main(int argc, char* argv[])
{
    std::cout << "SenseVoice inference test stub." << std::endl;
    std::cout << "Use the iOS app (SenseVoiceContext) for actual inference." << std::endl;
    return 0;
}
